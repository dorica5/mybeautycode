# myne Monorepo

Beauty-care platform (mobile app + API + marketing site).

## Apps

- `apps/backend` — Node/Express API (Prisma + Supabase)
- `apps/mobile/myne` — React Native / Expo app (package name: `myne`)
- `apps/web` — Next.js marketing site (myne.no)
